---
export const prerender = true;

import BaseLayout from "@/layouts/BaseLayout.astro";
import Statusbar from "@/components/Statusbar";
import Nav from "@/components/Nav.astro";
import Hero from "@/components/Hero.astro";
import Readme from "@/components/Readme.astro";
import Changelog from "@/components/Changelog.astro";
import ProjectsSection from "@/components/ProjectsSection.astro";
import PackageJson from "@/components/PackageJson.astro";
import Now from "@/components/Now.astro";
import ContactSection from "@/components/ContactSection.astro";
import Footer from "@/components/Footer.astro";
import Terminal from "@/components/Terminal";
import ChatBot from "@/components/ChatBot";
import { getCollection } from "astro:content";

const allProjects = await getCollection("projects", ({ data }) => !data.draft);
const terminalProjects = allProjects
  .sort((a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf())
  .map((p) => ({ name: p.data.name, tagline: p.data.tagline }));
---
<BaseLayout>
  <div class="app">
    <Statusbar client:load />
    <Nav />
    <main class="page" id="top">
      <Hero />
      <Readme />
      <Changelog />
      <ProjectsSection />
      <PackageJson />
      <Now />
      <ContactSection />
      <Footer />
    </main>
    <Terminal client:idle projects={terminalProjects} />
    <ChatBot client:idle />
  </div>
</BaseLayout>
